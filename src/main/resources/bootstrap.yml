server:
  port: 3100

spring:
  application:
    name: config-server

  profiles:
    active: git

  cloud:
    config:
      server:
        git:
          uri: https://github.com/ThxForService/configServer.git
          searchPaths: configs
          default-label: master
encrypt:
  key: ${secretKey}